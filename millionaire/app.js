import {ALL_COUNTRIES} from './countries.js';import {validateAmount,rankMillionaireCountries,formatMoney,buildShareText} from './core.js';
const $=s=>document.querySelector(s),form=$('#money-form'),amountInput=$('#amount'),currencySelect=$('#currency'),landing=$('#landing'),loading=$('#loading'),errorBox=$('#error'),result=$('#result'),heroFlag=$('#hero-flag'),heroCountry=$('#hero-country'),heroMoney=$('#hero-money'),heroOriginal=$('#hero-original'),count=$('#country-count'),others=$('#others'),rateDate=$('#rate-date'),shareX=$('#share-x'),copy=$('#copy'),again=$('#again');let currentShare='';
const currencies=[...new Set(ALL_COUNTRIES.map(x=>x.currency))].sort();for(const code of currencies){const option=document.createElement('option');option.value=code;option.textContent=code;currencySelect.append(option)}currencySelect.value='USD';
function setView(name){for(const el of[landing,loading,errorBox,result])el.hidden=true;$(`#${name}`).hidden=false}
async function fetchRates(base){const response=await fetch(`https://api.frankfurter.dev/v2/rates?base=${encodeURIComponent(base)}`);if(!response.ok)throw new Error('Exchange-rate service is unavailable right now.');const rows=await response.json(),rates=Object.fromEntries(rows.map(row=>[row.quote,row.rate]));rates[base]=1;return{rates,date:rows.map(r=>r.date).filter(Boolean).sort().slice(-1)[0]||'latest available'}}
async function calculate(amountValue,currency){try{const amount=validateAmount(amountValue);setView('loading');const{rates,date}=await fetchRates(currency),ranked=rankMillionaireCountries(amount,rates,ALL_COUNTRIES);if(!ranked.length)throw new Error('Not a millionaire anywhere yet. Try a bigger number.');const hero=ranked[0];heroFlag.textContent=hero.flag;heroCountry.textContent=hero.country;heroMoney.textContent=formatMoney(hero.localAmount,hero.currency);heroOriginal.textContent=`${formatMoney(amount,currency)} makes you a millionaire here.`;count.textContent=ranked.length===1?'1 country unlocked':`${ranked.length} countries unlocked`;rateDate.textContent=`FX data: ${date} · Frankfurter`;others.replaceChildren(...ranked.slice(1).map(item=>{const li=document.createElement('li');li.innerHTML=`<span class="other-country"><b>${item.flag}</b> ${item.country}</span><strong>${formatMoney(item.localAmount,item.currency)}</strong>`;return li}));currentShare=buildShareText(hero,amount,currency);const url=new URL(location.href);url.search='';url.searchParams.set('amount',amount);url.searchParams.set('currency',currency);history.replaceState(null,'',url);setView('result')}catch(error){$('#error-message').textContent=error.message||'Something went wrong.';setView('error')}}
form.addEventListener('submit',e=>{e.preventDefault();calculate(amountInput.value,currencySelect.value)});$('#retry').addEventListener('click',()=>calculate(amountInput.value,currencySelect.value));again.addEventListener('click',()=>{history.replaceState(null,'',location.pathname);setView('landing');amountInput.focus()});shareX.addEventListener('click',()=>window.open(`https://twitter.com/intent/tweet?text=${encodeURIComponent(currentShare)}&url=${encodeURIComponent(location.href)}`,'_blank','noopener,noreferrer'));copy.addEventListener('click',async()=>{await navigator.clipboard.writeText(`${currentShare} ${location.href}`);const old=copy.textContent;copy.textContent='Copied!';setTimeout(()=>copy.textContent=old,1400)});
const params=new URLSearchParams(location.search);if(params.has('amount')){amountInput.value=params.get('amount');const requested=params.get('currency');if(currencies.includes(requested))currencySelect.value=requested;calculate(amountInput.value,currencySelect.value)}
